{% extends 'Back/base.html.twig' %}

{% block title %}AdministrativeArea{% endblock %}

{% block body %}
    <h1>AdministrativeArea</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ administrative_area.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ administrative_area.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ administrative_area.code }}</td>
            </tr>
            <tr>
                <th>ParentCode</th>
                <td>{{ administrative_area.parentCode }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ administrative_area.type }}</td>
            </tr>
            <tr>
                <th>Details</th>
                <td>{{ administrative_area.details ? administrative_area.details|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('administrative_area_index') }}">Retour à la liste</a>

    <a href="{{ path('administrative_area_edit', {'id': administrative_area.id}) }}">edit</a>

    {{ include('Back/crud/administrative_area/_delete_form.html.twig') }}
{% endblock %}
