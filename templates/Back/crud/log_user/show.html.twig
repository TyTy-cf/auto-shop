{% extends 'Back/base.html.twig' %}

{% block title %}LogUser{% endblock %}

{% block body %}
    <h1>LogUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ log_user.id }}</td>
            </tr>
            <tr>
                <th>TargetEntity</th>
                <td>{{ log_user.targetEntity }}</td>
            </tr>
            <tr>
                <th>TargetEntityType</th>
                <td>{{ log_user.targetEntityType }}</td>
            </tr>
            <tr>
                <th>LogAt</th>
                <td>{{ log_user.logAt ? log_user.logAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Action</th>
                <td>{{ log_user.action }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('log_user_index') }}">back to list</a>

    <a href="{{ path('log_user_edit', {'id': log_user.id}) }}">edit</a>

    {{ include('Back/crud/log_user/_delete_form.html.twig') }}
{% endblock %}
