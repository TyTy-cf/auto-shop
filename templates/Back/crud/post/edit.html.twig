{% extends 'Back/base.html.twig' %}

{% block title %}Edit Post{% endblock %}

{% block body %}
    <div class="text-end">
        <a href="{{ path('post_index') }}">
            Retour à la liste
        </a>
    </div>

    <h1>Modifier {{ post.title }}</h1>

    {{ include('Back/crud/post/_form.html.twig', {'button_label': 'Modifier'}) }}

    <div class="text-end">
        {{ include('Back/crud/post/_delete_form.html.twig') }}
    </div>

{% endblock %}
